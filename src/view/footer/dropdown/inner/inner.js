import ElementCreator from '../../../../util/element-creator.js';import styles from './inner.module.scss';import svg from '../../../../assets/arrow.svg';import View from '../../../../util/view.js';export class Inner extends View {    constructor() {        /**         * @type {import('../../../../util/element-creator.js').ElementParams} params         */        const params = {            tag: 'div',            classNames: [styles.inner, 'dropdown-inner'],        };        super(params);        this.configureView();    }    configureView() {        const inner = this.elementCreator.getElement();        this.toggle = new ElementCreator({ tag: 'button', classNames: [styles.toggle] }).getElement();        inner.append(this.toggle);        this.input = new ElementCreator({            tag: 'input',            classNames: [styles.input, 'dropdown-input'],        }).getElement();        this.toggle.append(this.input);        this.toggle.insertAdjacentHTML('beforeend', `${svg}`);    }    setEventListenerIsShow(callback) {        this.toggle.addEventListener('click', callback);    }    setInput(value) {        this.input.value = value;    }}